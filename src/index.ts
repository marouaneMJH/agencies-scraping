import { sortListAgencies, test } from "./scrapeFullStackCompanies";
import { specialtys } from "@/common/constants/sort-list.constant";
(async () => {
    // Call the function to sort the list of agencies
    // await sortListAgencies(specialtys);

    await test();
})();
